import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 64,
    backgroundColor: '#262626',
    borderRadius: 8,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    borderWidth: 1,
    borderColor: '#333333',
    marginBottom: 8,
  },
  message: {
    fontSize: 14,
    color: '#f2f2f2',
    flex: 1,
  },
});
